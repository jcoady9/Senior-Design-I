//Convert Vec4i into vertexes.  Run vertexes through DFS.

#include "../include/dfs.h"
#include "../include/vertex.h"

void Vec2Vertex(vector<Vec4i> &vec)
{
	Vertex v1 = new Vertex(vec[0], vec[1]);
	Vertex v2 = new Vertex(vec[2], vec[3]);
}
