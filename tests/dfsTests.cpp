#include <stdio.h>
#include "../src/dfs.cpp"
#include "../src/vertex.cpp"

int main()
{
	Vertex array[]; 	
	Vertex v; 
	//TODO: need to know structure of vertex class before we can continue.
	array[1] = v; 
	array[1] = v; 
	array[1] = v; 
	
	DFS(graph);

	//print graph
	
	return 0
}
